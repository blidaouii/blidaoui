# Icon source

Place the final branded Ludo DZ launcher artwork at:

`assets/icon/ludo_dz_icon.png`

The `pubspec.yaml` contains the launcher source configuration. Generate platform-specific launcher sizes using your release pipeline (for example, `flutter_launcher_icons` in a controlled build environment); generated Android resources are intentionally not committed.
